spring:
  main:
    banner-mode: off
  application:
    name: space-flight-news-integration
  profiles:
    active: test
  batch:
    job:
      enable: false

integration:
  error:
    topic: integration.error.topic

scheduling:
  fix-rate: 3600000

space:
  flight:
    server:
      url: http://localhost:9000
      port: 9000

server:
  port: 8080