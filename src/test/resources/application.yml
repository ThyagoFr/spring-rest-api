spring:
  main:
    banner-mode: off
  application:
    name: space-flight-news-integration
  profiles:
    active: test

scheduling:
  enable: false

space:
  flight:
    news-api:
      url: https://api.spaceflightnewsapi.net/v3/articles

server:
  port: 8080